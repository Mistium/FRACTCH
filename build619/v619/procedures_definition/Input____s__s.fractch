/**
 * target: v619
 * targetId: 
 * topBlockId: acB
 * hatOpcode: procedures_definition
 * threadIndex: 104
 */

def @Input(cursorchange, key) "Input// %s %s" warp {
  if vars["Key// Current Pressed"] == key {
    if vars["Inputs // Cursor Position"] + cursorchange > -1 {
      vars["Inputs // Cursor Position"] += cursorchange;
    }
  }
}
