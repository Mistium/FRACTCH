/**
 * target: v619
 * targetId: 
 * topBlockId: aPN
 * hatOpcode: procedures_definition
 * threadIndex: 217
 */

def @Elements_New_With_Name(name) "Elements // New With Name %s" warp=false {
  mistsutils.patchcommand(A: "let temp = document.createElement(\"div\")");
  mistsutils.patchcommand(A: "temp.id = p0 + \"_OSLoverlay\"");
  mistsutils.patchcommand(A: "temp.style.display = \"none\"");
  mistsutils.patchcommand(A: "temp.style.position = \"absolute\"");
  mistsutils.patchcommand(A: "origin.elements[p0] = temp;");
  mistsutils.patchcommand(A: "vm.renderer.addOverlay(temp);");
}
