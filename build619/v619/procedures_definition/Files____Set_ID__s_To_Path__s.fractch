/**
 * target: v619
 * targetId: 
 * topBlockId: aJV
 * hatOpcode: procedures_definition
 * threadIndex: 186
 */

def @Files_Set_ID_To_Path(ID, Path) "Files // Set ID %s To Path %s" warp=false {
  mistsutils.patchcommand(A: "const paths = window?.files?.paths");
  mistsutils.patchcommand(A: "const path = (\"\"+p1).toLowerCase()");
  mistsutils.patchcommand2(A: "const oldPath = ", B: data.itemoflist(INDEX: ID, field LIST: list("Files // Index Path", "CL33%0R^/cfdOsUUuN{9")));
  mistsutils.patchcommand(A: "delete paths[oldPath]");
  data.replaceitemoflist(INDEX: ID, ITEM: Path, field LIST: list("Files // Index Path", "CL33%0R^/cfdOsUUuN{9"));
  mistsutils.patchcommand(A: "paths[path] = p0");
}
