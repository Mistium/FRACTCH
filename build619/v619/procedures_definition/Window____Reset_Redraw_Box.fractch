/**
 * target: v619
 * targetId: 
 * topBlockId: ]p
 * hatOpcode: procedures_definition
 * threadIndex: 73
 */

def @Window_Reset_Redraw_Box() "Window // Reset Redraw Box" warp {
  vars["temp"] = vars["Screen Size X"] / 2;
  vars["Redraw // Window Left"] = 0 - temp;
  vars["Redraw // Window Right"] = temp;
  vars["temp"] = vars["Screen Size Y"] / 2;
  vars["Redraw // Window Bottom"] = 0 - temp;
  vars["Redraw // Window Top"] = temp;
}
