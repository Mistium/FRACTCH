/**
 * target: v619
 * targetId: 
 * topBlockId: ar|
 * hatOpcode: procedures_definition
 * threadIndex: 149
 * pos: 107819,64
 */

def @OSL(object, type, name, value) "OSL // %s .( %s ) %s = %s" {
  mistsutils.patchcommand(A: "if (!p1 || p1 === \"\") { p0[\"\"+p2] = p3 ?? null; return; }");
  mistsutils.patchcommand(A: "defineTypedProperty(p0, \"\"+p2, p3 ?? null, (\"\"+p1).toLowerCase(), target)");
}
