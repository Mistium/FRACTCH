/**
 * target: v619
 * targetId: 
 * topBlockId: aMC
 * hatOpcode: procedures_definition
 * threadIndex: 194
 */

def @originWM_Send_global_Msg(arr) "originWM // Send global Msg %s" warp=false {
  mistsutils.patchcommand2(A: "p0 = typeof p0 === \"object\" ? p0 : ", B: OSLUtils.tokeniseValues(CODE: arr, DELIMITER: "/"));
  mistsutils.patchcommand(A: "const id = parseInt(String(p0[1]))");
  if mistsutils.patchboolean(A: "window.origin.windows.ids.filter(v => v >= 0).includes(id)") {
    mistsutils.patchcommand(A: "window.origin.sudoTransmit.push({id, time: Date.now(), data: p0})");
  }
}
