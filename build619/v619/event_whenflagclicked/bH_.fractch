/**
 * target: v619
 * targetId: 
 * topBlockId: bH#
 * hatOpcode: event_whenflagclicked
 * threadIndex: 195
 */

event.whenflagclicked();
forever {
  mistsutils.patchcommand(A: "const tra = window?.origin?.sudoTransmit");
  mistsutils.patchcommand(A: "if (!tra?.length) { yield; continue }");
  mistsutils.patchcommand(A: "for (let i = 0; i < tra.length; i ++) {");
  vars["SYSTEM // Global Message"] = mistsutils.patchreporter(A: "tra.shift().data");
  broadcast_wait "Send Global Message";
  mistsutils.patchcommand(A: "}");
}
